query GetRehearsalAttendanceStats($rehearsalId: ID!) {
    Rehearsal {
        getAttendanceStats(rehearsalId: $rehearsalId) {
            totalMembers
            attendedMembers
            attendancePercentage
        }
    }
}
