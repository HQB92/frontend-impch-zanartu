query GetSummaryOfferings($mes: Int, $anio: Int,$churchId: Int) {
    Offering {
        getSummaryAll(mes: $mes, anio: $anio, churchId: $churchId) {
            churchId
            name
            total
            count
        }
    }
}

